# ─────────────────────────────────────────────
#  configs/inference.yaml
#  Configuration for the standalone inference script
# ─────────────────────────────────────────────

model_checkpoint: "outputs/banking-intent/best_model"   # path saved by train.py
label_map: "sample_data/label_map.yaml"
max_seq_length: 128
load_in_4bit: true
device: "auto"    # "auto" | "cuda" | "cpu"
